# CLAUDE.md — Denkeez(仮) プロジェクト作業ルール

> このファイルは Claude Code がこのリポジトリで作業する際に、毎回参照する設定書です。
> プロジェクトの全体像・コーディング規約・進め方の作法をまとめています。

---

## このプロジェクトについて

**Denkeez(デンキーズ・仮称)** は、電気工事業者向けの図面エディタです。元請けから来る平面図(PDF)の上に、照明・スイッチ・コンセントなどの記号を配置し、拾い出し(数量集計)から見積連携までを支援するデスクトップアプリケーションです。

詳細は `REQUIREMENTS.md` を参照してください。**作業を始める前に必ず一度通読すること**。

## 必読ドキュメント

作業内容によって、以下を参照してから着手してください。

| 状況 | 必ず読むもの |
|---|---|
| はじめてこのリポジトリで作業する | `README.md`, `REQUIREMENTS.md`, `CLAUDE.md`(本書) |
| 機能を実装する | `REQUIREMENTS.md` の該当セクション |
| 仕様について判断に迷う | `REQUIREMENTS.md` §9.3 仕様未確定事項 |
| ユーザー視点を確認したい | `docs/搭載予定機能のご紹介.docx` |

## 基本方針

### 1. Plan ファースト

実装に着手する前に、**必ず Plan モードで設計を提示**してください。Daisuke の承認を得てから実装に進みます。

特に Phase 1 (PoC) では:
- ファイル構成・ディレクトリ構成の提案
- 主要な技術選定の判断材料
- リスクの洗い出し

を先に提示してください。

### 2. 仕様未確定事項は勝手に決めない

`REQUIREMENTS.md` §9.3 に「仕様未確定事項」がリストされています。これらに該当する判断が必要になったら、**実装に進む前に Daisuke に確認**してください。

確認時は以下を含めてください:
- 検討した選択肢
- それぞれのメリット・デメリット
- 推奨する案とその理由

### 3. 動かない可能性のある前提を先に洗い出す

「やってみたら動かなかった」を避けるため、リスクのある前提は事前に小さなサンプルで検証してください。例:

- PDF.js で大きな PDF を扱った時の性能
- Konva.js での座標系の扱い
- Tauri / Electron の選定に関わる挙動

## 技術スタック

### 確定事項

| 領域 | 採用技術 |
|---|---|
| 言語 | TypeScript (strict mode) |
| フロントエンド | React |
| ビルド | Vite |

### 仮置き(Phase 1 着手時に最終決定)

| 領域 | 候補 |
|---|---|
| アプリ形態 | Electron または Tauri |
| 描画エンジン | Konva.js |
| PDF 表示 | PDF.js |
| データ保存 | SQLite または JSON ファイル |
| 状態管理 | Zustand or Redux Toolkit |

仮置きの選定を確定する前に、Daisuke と協議してください。既存の見積管理アプリのスタックに合わせる方針です。

## コーディング規約

### TypeScript

- `strict: true` を有効化
- `any` の使用は原則禁止(やむを得ない場合はコメントで理由を残す)
- 副作用を持つ関数と純粋関数を明確に分離

### コードスタイル

- ESLint + Prettier 適用
- 1 ファイル 300 行以内を目安に分割
- 変数名・関数名は **英語**(意味が明確に伝わる名前)
- コメントは **日本語可**(むしろ複雑な業務ロジックは日本語で書いたほうが分かりやすい)

### 命名規約

- ファイル名: `kebab-case` (例: `pdf-loader.ts`)
- React コンポーネントファイル: `PascalCase.tsx` (例: `SymbolPalette.tsx`)
- 定数: `UPPER_SNAKE_CASE`
- 変数・関数: `camelCase`
- 型・インターフェース: `PascalCase`

### 重要: アプリ名は定数化する

アプリ名「Denkeez」は仮称のため、UI 表示で使う文字列は必ず定数経由で参照すること。

```typescript
// src/constants/app.ts
export const APP_NAME = "Denkeez";
export const APP_NAME_KANA = "デンキーズ";
export const APP_NAME_DISPLAY = `${APP_NAME}(${APP_NAME_KANA}・仮称)`;
export const APP_FILE_EXTENSION = "dkz"; // 仮
```

UI 文字列に直接「Denkeez」と書かず、必ずこの定数を import して使ってください。

## ディレクトリ構成(参考)

Phase 1 着手時に確定しますが、目安として:

```
denkeez/
├── src/
│   ├── main/              ← Electron/Tauri メインプロセス
│   ├── renderer/          ← React アプリ本体
│   │   ├── components/    ← UI コンポーネント
│   │   ├── canvas/        ← 描画エンジン関連
│   │   ├── data/          ← データモデル・永続化
│   │   ├── symbols/       ← シンボル定義
│   │   └── utils/         ← ユーティリティ(座標変換など)
│   └── shared/            ← main/renderer 共通の型定義
├── docs/
├── public/
├── tests/
└── ...
```

## ブランチ運用

- `main` には直接 push しない
- 機能ごとに `feature/xxx` ブランチを作成
  - 例: `feature/pdf-loader`, `feature/symbol-palette`
- バグ修正は `fix/xxx` ブランチ
- ドキュメント更新のみは `docs/xxx` ブランチでも可

### コミットメッセージ

日本語可。以下のプレフィックスを目安に使ってください:

- `追加:` 新機能
- `修正:` バグ修正
- `改善:` リファクタリング・軽微な改良
- `文書:` ドキュメントのみの変更
- `設定:` ビルド設定・依存関係の更新
- `テスト:` テストコードの追加・修正

例:
```
追加: PDF 読み込み機能の基本実装

- PDF.js を使って単一ページ PDF をキャンバスに表示
- 複数ページ対応は Phase 2 以降
```

### Pull Request

- 機能が完成したら main 宛に PR を出す
- PR の説明には「何を実装したか」「テスト方法」「未実装/今後の予定」を記載
- レビュー OK 後に Daisuke がマージする運用(現状)

## テスト方針

### Phase 1 (PoC) では

- 手動テストが中心で OK
- ただし座標変換ユーティリティなど、ロジックが複雑な部分は単体テストを書く

### Phase 2 (MVP) 以降

- 描画エンジン・データモデル・拾い出しロジックには単体テスト必須
- E2E テストは Playwright を候補として検討

## 注意事項・既知の難所

`REQUIREMENTS.md` §9 に詳細あり。特に以下は設計時から意識すること:

1. **座標系の二重管理** — スクリーン座標(px) ↔ 実寸座標(mm) の変換を必ずユーティリティに集約。UI コードから直接ピクセル計算しない。
2. **アンドゥ・リドゥ** — Command パターンか状態スナップショットかを早期決定
3. **大きな PDF のメモリ管理** — A1 サイズの PDF も扱える前提で設計
4. **配線とシンボルの参照整合性** — シンボル削除時の配線の扱いを定義
5. **シンボル ID は UUID** — 再生成しないこと

## ユーザー視点で大切にしたいこと

このアプリは **電気工事職人(Daisuke と異なるバックグラウンドの方)** が使います。実装時は以下を意識してください:

- 専門的な操作を強制しない(マウス操作で完結することを優先)
- エラーメッセージは日本語で、何が起きたか・どうすればいいかを書く
- 「保存し忘れて全部消えた」は絶対に避ける(自動保存・復旧機能は最初から)
- キーボードショートカットは Windows 標準に従う(Ctrl+S, Ctrl+Z など)

詳しいユーザー像と機能の優先度は `docs/搭載予定機能のご紹介.docx` を参照。

## 困ったときの判断基準

仕様や設計で迷ったときは、以下の優先順位で判断してください。

1. **`REQUIREMENTS.md` に明記されている** → それに従う
2. **`REQUIREMENTS.md` §9.3 の仕様未確定事項に該当** → Daisuke に確認
3. **要件定義書に書いていない** → 「職人さんの作業を楽にするか」を基準に推奨案を提示してから着手
4. **技術選定で迷う** → 既存見積管理アプリとの整合性を最優先

## このファイルの更新

開発を進める中で「こうしたほうがいい」というルールが見つかったら、このファイルを Pull Request で更新してください。プロジェクトの作法は育てていくものです。
# 開発ルール（Claude Code / AI コーディング向け）

> 参考: 伊勢川 暁氏「エンジニア歴20年の私が、素人バイブコーディング勢に物申す」(Qiita)
> 本ルールは、AIアシスタント（Claude Code等）にコードを書かせる際の遵守事項。
> **AIに作業を依頼する前にこのファイルを必ず読み込ませること。**

---

## 0. このプロジェクトでの大原則

- **このプロジェクトのコードは「本番で他人に使われる可能性のあるソフトウェア」である**ことを常に前提にする。
- 「動いた」≠「リリースしてよい」。動作確認後に **必ず本ルールのチェックリストを通過させる**。
- ルールに反する実装を提案する場合は、**先に「ルール◯条に抵触します」と明示**してから代替案を出すこと。
- 不明点は推測で進めず、**ユーザー（開発依頼者）に質問する**。

---

## 1. セキュリティ（最優先・絶対遵守）

### 1.1 シークレット管理
- APIキー・秘密鍵・トークンを **コード・コメント・プロンプト・チャット履歴に直書きしない**。
- 必ず環境変数（`.env`）または Secrets Manager に格納する。
- `.env` `.env.local` は `.gitignore` に必ず登録。コミット前に `git status` で確認する。
- 公開鍵（クライアント側で使ってよい）と秘密鍵（サーバー側のみ）を混同しない。
- テスト用と本番用で鍵を分ける。同じ鍵を使い回さない。
- ログ・エラー・スクリーンショット・AI質問文に鍵が含まれていないか毎回チェック。

### 1.2 認可（IDOR対策）
- データを取り出すAPI / クエリは **必ず「ログイン中のユーザーが所有しているか」をサーバー側で検証する**。
- URL に ID が含まれる場合（例：`/estimates/123`）は、URL上のIDだけを信用せず、必ず `userId === session.userId` のチェックを書く。
- AIが生成したAPIコードは、`if (record.userId !== currentUser.id) return 403` 相当のガードがあるかを毎回確認すること。

### 1.3 入力検証
- ユーザー入力・外部APIレスポンス・アップロードファイルは **すべて「敵かもしれない」前提で扱う**。
- **クライアント側のバリデーションだけで終わらせない**。サーバー側（API Route / Edge Function）でも必ず同じ検証を行う。
- バリデーションは Zod / Yup / valibot 等のスキーマライブラリで一元管理する。
- ファイルアップロード時:
  - 拡張子だけでなく **MIMEタイプとマジックナンバー** の3点で判定。
  - ファイル名をそのままパスに使わない（パストラバーサル対策）。
  - サイズ上限を必ず設定。
  - SVGはサニタイズしてから表示（XSS対策）。

### 1.4 ライブラリの導入
新しいライブラリを `npm install` する前に、必ず以下を確認:
- [ ] npm 公式に実在するパッケージか（ハルシネーション対策）
- [ ] 最終更新が直近1年以内か
- [ ] GitHub のスター数・Issue 状況・メンテナの活動
- [ ] ライセンス（GPL系は商用利用注意）
- [ ] `package-lock.json` でバージョン固定されているか

### 1.5 エラーメッセージ
- 本番環境で **スタックトレース・SQL文・内部パス・テーブル名をユーザーに返さない**。
- ユーザー向けメッセージは「処理に失敗しました」程度に抽象化。詳細はサーバーログにのみ残す。
- フレームワークの「開発モード / 本番モード」切り替えを必ず実装。

### 1.6 ログ
- アクセスログ・エラーログを必ず残す。
- ログに **パスワード・APIキー・クレジットカード番号・氏名・メール・住所などの個人情報を出力しない**。
- 異常検知時のアラート（Sentry等）を設定する。

### 1.7 セキュリティヘッダ
- HTTPS必須（HSTS有効化）。
- `Content-Security-Policy`、`X-Frame-Options`、`X-Content-Type-Options: nosniff` を設定。
- CORS設定で `*` と credentials を組み合わせない。

---

## 2. コスト管理

### 2.1 実装前
- 外部API（LLM、画像生成、Firestore等）を使う前に **料金体系を確認**:
  - 課金単位（リクエスト / トークン / 読み取り回数 / ストレージ）
  - 「1日100ユーザー × 10回利用」での月額試算
  - 無料枠の終了条件
  - ハードリミット（強制停止）の設定可否

### 2.2 実装時
- LLM API 呼び出しに **タイムアウト・リトライ上限・最大トークン数** を必ず設定。
- ユーザーが連打できるボタンには **レートリミット**（クライアント側のdebounce + サーバー側のレート制限）を実装。
- 無限ループ・再帰での外部API呼び出しが発生しないよう必ずレビューする。

### 2.3 リリース後
- 予算アラートを **コードを書く前に** 設定する。
- 請求ダッシュボードをリリース直後・週次・月次で確認する習慣をルールに含める。

---

## 3. 法的・コンプライアンス

コーディング前に確認すること:
- スクレイピング対象サイトの利用規約に違反しないか
- 個人情報を扱う場合、個人情報保護法・GDPR の要件を満たすか
- 使用ライブラリのライセンス（GPL汚染に注意）
- AI生成画像・文章の著作権リスク
- 医療・金融・法律など業法規制のある領域に踏み込んでいないか

---

## 4. データ設計（コードより重い）

> **コードのバグは直せる。データ構造の負債は、後から払うと利息がついて返ってくる。**

### 4.1 設計時に明文化すること
- 扱うエンティティ（例: User / Estimate / EstimateItem / Customer）を日本語で言語化
- 関係性（1対多 / 多対多）を ER 図またはコメントで明示
- 将来増えそうな項目は別テーブル化を検討
- 削除は **物理削除か論理削除か** を最初に決める

### 4.2 マイグレーション運用
- 本番DBには **自動バックアップを設定**（最初に必ずやる）。
- マイグレーションは **必ずステージング環境で試してから本番適用**。
- カラム削除・型変更などの破壊的変更は **サービス停止のうえで実施**（無停止デプロイは熟練者向け）。
- 本番DBを SQL クライアントで直接操作しない。必ずマイグレーションスクリプト経由で。

---

## 5. トランザクション・冪等性

AIにロジックを書かせたら、**毎回以下を聞き返す**:
- 「途中で落ちたら何が起きる？」
- 「同じリクエストが2回来たら何が起きる？」

### 5.1 トランザクション境界
- 複数テーブルへの書き込みは1つのDBトランザクションでまとめる。
- 「全部成功するか、全部なかったことにするか」を明示。

### 5.2 冪等性
- 決済・課金・外部API呼び出しには **冪等性キー (Idempotency Key)** を設計に組み込む。
- フォーム送信ボタンは二重送信対策（disabled / debounce / サーバー側の重複検出）。

---

## 6. テスト

### 6.1 テストを書かせる手順
AIに丸投げせず、以下のステップで指示する:
1. 「実装した部分の仕様書を作って」 → 仕様の網羅性を確認
2. 「仕様書から網羅的なテストケースを洗い出し、自動テスト範囲と人手確認範囲を整理して」
3. 「自動テスト範囲を実装して」
4. 「結合テストすべきケースを洗い出して」
5. 「結合テストを実装して」
6. 「◯◯の処理の後に✗✗をしたらどうなる？テストしてる？」と追加確認

### 6.2 テストレビュー観点
- [ ] テスト名が仕様になっているか（`test_1` ではなく「金額が0円のとき注文を拒否する」）
- [ ] Arrange / Act / Assert が分離されているか
- [ ] モックだらけになっていないか（モックを呼んだことしか検証していない状態を避ける）
- [ ] ハッピーパスだけでなく **異常系・境界値** をカバーしているか
- [ ] 実装の詳細（プライベートメソッド・内部変数）に依存していないか

---

## 7. 保守性

- **DRY**: 同じロジックを2箇所以上に書かない。
- AIが書いたコードでも、**自分の言葉で「これは何をしているか」を説明できる状態**にする。
- 説明できないコードはマージしない・リリースしない。
- マジックナンバー・マジックストリングは定数化。
- 関数は1つのことだけをする。引数は理想0〜2個。
- ネストは浅く、早期リターンを使う。

---

## 8. Git 運用

- 小さく、頻繁にコミットする。
- コミットメッセージには **「何を」「なぜ」変えたか** を書く（Conventional Commits 推奨: `feat:` `fix:` `refactor:` `docs:` `test:`）。
- `.gitignore` でシークレット・`node_modules`・ビルド成果物を除外。
- リポジトリは最初は **必ず Private**。
- AI に大きな変更を依頼する前に必ずコミット or ブランチを切る（巻き戻せるように）。

---

## 9. 環境分離

- **開発（ローカル）/ ステージング / 本番** を分ける。
- データベース・APIキー・外部サービスもすべて環境別に分離。
- 「本番でちょっと試す」は禁止。
- `.env.development` `.env.staging` `.env.production` を使い分ける。

---

## 10. パフォーマンス

実装時に最低限意識すること:
- N+1問題（ループ内でDB問い合わせをしない、`include` / `JOIN` を使う）
- インデックス設計（よく検索される条件にはインデックス）
- `SELECT *` を避け、必要なカラムだけ取得
- ページネーション（オフセット型は件数増加で劣化、件数が多くなるならカーソル型）
- フロントエンドは Core Web Vitals（LCP / INP / CLS）を意識

「いま自分が書いているコードは何件まで耐える設計か」を、ぼんやりとでも把握してから書く。

---

## 11. インシデント対応

万が一のとき、この順番で動く:
1. **まず止める** — アプリ停止、APIキー無効化、被害拡大を防ぐ
2. **記録する** — 発生時刻・検知経緯・分かっていることをメモ
3. **影響を受けた人に早く知らせる** — 「調査中です」でよい。沈黙が最悪
4. **原因を突き止める** — 表面ではなく構造的原因まで掘る
5. **再発策を仕組みで作る** — テスト追加、アラート見直し、設計修正

---

## 12. AIへの依頼時の標準手順

このプロジェクトで Claude Code / AI に作業を依頼するときの定型フロー:

1. **依頼前**: 関連ファイルを AI に読ませる（このルールファイル含む）
2. **設計確認**: 「実装する前に、設計案と影響範囲を箇条書きで出して」
3. **実装**: 設計合意後にコード生成
4. **セキュリティチェック**: 「このコードに本ルールに反する箇所はないか自己レビューして」
5. **テスト**: 上記6.1の手順で
6. **ユーザーレビュー**: 必ず人間（あなた）が読んで理解する。説明できないものはマージしない
7. **コミット**: 小さく、メッセージを丁寧に

---

## 13. 禁止事項（このプロジェクトで絶対にやらない）

- ❌ APIキー・秘密鍵をコードにハードコード
- ❌ クライアント側だけのバリデーションで済ませる
- ❌ サーバー側で認可チェックなしのデータ取得API
- ❌ 本番DBへの直接SQL実行
- ❌ バックアップなしのスキーマ変更
- ❌ 説明できないAI生成コードのマージ
- ❌ 予算アラートを設定する前の外部API利用
- ❌ `.env` のコミット
- ❌ 実在確認していないライブラリの導入
- ❌ 「動いたから本番リリース」（チェックリスト未通過）

---

## 14. リリース前チェックリスト

機能を本番にリリースする前に、必ず以下をすべて確認:

- [ ] 認可チェック（IDOR対策）が全データ取得APIに入っている
- [ ] サーバー側バリデーションがすべての入力に対して行われている
- [ ] シークレットがコードに含まれていない（`git grep` で確認）
- [ ] `.env` が `.gitignore` に入っている
- [ ] エラーメッセージが本番モードで抽象化されている
- [ ] 予算アラートが設定されている
- [ ] DB自動バックアップが有効
- [ ] ステージング環境で動作確認済み
- [ ] ログに個人情報・シークレットが出ていない
- [ ] 主要な異常系のテストが通っている
- [ ] レートリミットが設定されている（外部API呼び出しを伴うエンドポイント）
- [ ] CSP・HSTS等のセキュリティヘッダが設定されている

---

## 付録: 困ったらAIに聞く観点キーワード

詰まったら、以下のキーワードを Claude / AI に「初心者にも分かるように教えて」と聞く:

**セキュリティ**: IDOR / OWASP Top 10 / CSP / SSRF / プロンプトインジェクション / 最小権限
**データ**: トランザクション / 冪等性 / マイグレーション / 論理削除 / N+1
**設計**: SOLID / DRY / KISS / YAGNI / 関心の分離 / 値オブジェクト
**テスト**: テストピラミッド / Arrange-Act-Assert / モック使いすぎ問題 / 境界値分析
**運用**: 12 Factor App / SLO / 構造化ログ / カナリアリリース / フィーチャーフラグ

---

_このルールは生きたドキュメントです。プロジェクトの状況に応じて追記・修正してください。_
_最終更新: 2026-04-27_

---

**最終更新**: 2026-04-29
