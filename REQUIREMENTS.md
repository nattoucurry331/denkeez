# Denkeez（仮） 要件定義書

> 本書は Claude Code が設計・実装を進めるための仕様の起点となる文書です。
> 本書に明記されていない仕様は、開発着手前に確認・合意してから着手してください。
>
> **プロダクト名は仮称「Denkeez（デンキーズ）」**で進めます。第 1 段階（PoC）終了時点で正式名称を確定する予定。コード内のアプリ名表示文字列は定数 `APP_NAME` として一元管理し、将来の名称変更に備えること。

---

## 1. プロジェクト概要

### 1.1 名称（仮）
- **正式名称（仮）**：Denkeez（デンキーズ）
- **由来**:「電気（Denki）」を語幹とし、語尾を英字「-eez」とした造語。覚えやすさ・ロゴ映え・ドメイン取得可能性を考慮して選定。
- **読み方の併記ルール**：仮称運用期間中は、UI・ドキュメント・対外発信のすべてで「Denkeez（デンキーズ）」とカナを併記する。仮称解除のタイミングは第 1 段階（PoC）完了時を目安とする。
- **コーディング上の取扱い**：
  - リポジトリ名・パッケージ名・拡張子等は `denkeez` で統一
  - UI 表示用のアプリ名は定数 `APP_NAME` で一元管理（将来の改名に備える）
  - プロジェクトファイル拡張子は `.dkz`（仮）

### 1.2 背景
- 北海道の建設会社（有限会社 北嶺建設）の協力業者である電気工事の職人から、「元請けから来る平面図に照明・スイッチ・コンセントなどの位置を書き込めるツールが欲しい」との要望があった。
- 現状は紙図面に手書きで書き込み、写真撮影して共有・保存している。
- これをデスクトップアプリでデジタル化することで、修正のやり直し・図面の取り違えを減らし、拾い出し（数量算出）から見積作成までの流れを効率化する。

### 1.3 目的
1. 元請けから受領した平面図（PDF/画像）の上に、電気設備記号と配線を効率的に書き込めるエディタを提供する。
2. 配置したシンボルを自動集計し、拾い出し（数量算出）作業を自動化する。
3. 既存の見積管理アプリ（別プロジェクト）と連携し、図面 → 拾い出し → 見積の流れをシームレスにする。
4. 提出用 PDF として出力できる体裁を整える。

### 1.4 想定ユーザー
- **主たるユーザー**：電気工事を行う職人（協力業者）
- **使用環境**：事務所または自宅の Windows PC
- **PC スペック想定**：Windows 10/11、メモリ 8GB 以上、フル HD ディスプレイ前提
- **対象外**：現場での iPad/タブレット利用は本フェーズでは対象外（将来検討）

### 1.5 スコープ外
- DWG/DXF 形式の直接読み込み（必要なら元請けに PDF 化を依頼）
- リアルタイム複数人共同編集
- クラウド同期（Phase 3 以降で検討）
- iPad/タブレット対応（将来検討）
- 立面図・展開図への展開（平面図のみ対象）
- 法的な施工図審査適合性の保証（あくまで作業補助ツール）

---

## 2. 技術スタック

### 2.1 推奨スタック
| 領域 | 採用技術 | 補足 |
|---|---|---|
| アプリ形態 | デスクトップアプリ（Electron または Tauri） | 既存見積管理アプリと揃える |
| フロントエンド | React + TypeScript | |
| 描画エンジン | **Konva.js** | Canvas をオブジェクト指向で扱える。ドラッグ・選択・レイヤー管理に最適 |
| PDF 表示 | **PDF.js** | Mozilla 製、定番ライブラリ |
| PDF 出力 | jsPDF + svg2pdf.js | ベクター情報を保ったまま出力 |
| データ保存 | SQLite（better-sqlite3）または JSON ファイル | プロジェクトごとに 1 ファイル |
| ビルド | Vite | |
| 状態管理 | Zustand または Redux Toolkit | アンドゥ・リドゥ実装の容易さで選定 |

### 2.2 技術選定の判断基準
- 既存の見積管理アプリと同じスタックに揃えることを優先する。
- ライブラリ追加時は、保守性・ドキュメント量・最終更新日を確認すること。
- 重要な技術選定（Electron vs Tauri、Konva vs Fabric など）は実装着手前に Daisuke と合意する。

---

## 3. 機能要件

### 3.1 機能一覧と優先度

優先度は MoSCoW 法で表記。
- **M**（Must）：MVP に必須
- **S**（Should）：MVP 後すぐ実装
- **C**（Could）：余力があれば実装
- **W**（Won't）：本フェーズでは実装しない

| ID | 機能 | 優先度 | Phase |
|---|---|---|---|
| F-01 | プロジェクトの新規作成・保存・読み込み | M | 1 |
| F-02 | PDF 図面の取り込みと背景表示 | M | 1 |
| F-03 | JPG/PNG 画像の取り込みと背景表示 | M | 1 |
| F-04 | スケール設定（2 点間の実寸入力） | M | 2 |
| F-05 | シンボルパレットからの配置（クリック方式） | M | 1 |
| F-06 | 配置済みシンボルの選択・移動・削除・回転 | M | 2 |
| F-07 | 配線描画（始点シンボル → 終点シンボル） | M | 2 |
| F-08 | レイヤー管理（表示/非表示・ロック・色分け） | M | 2 |
| F-09 | 拾い出し集計（シンボル種別ごとの数量自動表示） | M | 2 |
| F-10 | プロパティパネル（選択シンボルの詳細編集） | M | 2 |
| F-11 | アンドゥ・リドゥ | M | 2 |
| F-12 | ズーム・パン・ミニマップ | M | 2 |
| F-13 | グリッド表示（910mm/455mm 切替） | S | 2 |
| F-14 | PDF 出力（用紙サイズ・向き指定） | M | 2 |
| F-15 | 拾い出し CSV 出力 | M | 2 |
| F-16 | 寸法計測ツール（2 点間距離・配線総長） | S | 3 |
| F-17 | 直角配線モード | S | 3 |
| F-18 | テキスト・注記の自由配置 | S | 3 |
| F-19 | テンプレート機能（よく使う配置パターンの保存・呼出） | C | 3 |
| F-20 | 自動整列（複数選択時の等間隔配置・揃え） | C | 3 |
| F-21 | 見積管理アプリとの連携 | S | 3 |
| F-22 | 印刷タイトル枠の自動挿入 | S | 3 |
| F-23 | DWG/DXF 取り込み | W | - |
| F-24 | クラウド同期 | W | - |

### 3.2 機能詳細

#### F-01 プロジェクト管理
- 1 プロジェクト = 1 現場 = 1 ファイル（拡張子：`.dkz`（仮））
- 内部構成：元図面ファイル + 書き込みデータ（JSON） + メタデータ
- 自動保存（5 分間隔）と手動保存。ファイル復旧機能を持つこと。

#### F-02, F-03 図面取り込み
- ファイル選択ダイアログまたはドラッグ&ドロップ
- 複数ページ PDF の場合：ページ選択 UI を表示し、選んだ 1 ページのみ取り込む
- 取り込み後はキャンバス背景に固定し、専用「元図面レイヤー」に格納
- 元図面レイヤーは初期状態でロックする

#### F-04 スケール設定
- ツールバーから「スケール設定」モードを起動
- 図面上の既知の距離を 2 点クリックで指定
- ダイアログで実寸（mm）を入力
- 以後、座標表示・寸法計測・拾い出しすべてがこのスケールに従う
- スケール未設定時は警告を表示

#### F-05 シンボル配置
- 左パレットからシンボル種別を選択
- キャンバス上でクリックすると配置（連続配置モード）
- 配置中は ESC キーで配置モード解除
- 配置時の初期回転角度・サイズは種別ごとのデフォルトに従う

#### F-06 シンボル編集
- 単一選択：クリック
- 複数選択：Shift+クリック または ドラッグで矩形選択
- 移動：ドラッグ または 矢印キー（1mm 刻み、Shift+矢印で 10mm）
- 回転：選択ハンドル または プロパティパネルで角度入力
- 削除：Delete キー

#### F-07 配線描画
- 配線モードで始点シンボルをクリック → 中継点を任意数クリック → 終点シンボルをクリックで確定
- 配線種別：天井ふところ（実線）/ 床下（破線）/ 壁内隠蔽（点線）/ 露出（実線・色違い）
- 配線にも回路番号・線種（VVF1.6/VVF2.0 など）を持たせる
- 配線総長を自動計算し拾い出しに反映

#### F-08 レイヤー管理
- 標準レイヤー：元図面（ロック）/ 照明回路 / コンセント回路 / 弱電 / 換気・空調 / 寸法・注記
- ユーザーによるカスタムレイヤー追加・削除可能（元図面レイヤーは削除不可）
- 各レイヤーで：表示 ON/OFF・ロック・色設定・名前変更
- 出力時にレイヤーを選択できる

#### F-09 拾い出し集計
- 右下パネルに常時表示
- シンボル種別ごとに自動集計
- 配線は線種別に総長を表示
- レイヤーフィルタ可能（特定レイヤーのみ集計）

#### F-10 プロパティパネル
- 選択中シンボル / 配線の詳細を表示・編集
- シンボル種別ごとに表示項目が異なる（後述「シンボル定義」参照）
- 何も選択していない場合は図面全体のプロパティ（縮尺・用紙サイズ・グリッド）を表示

#### F-11 アンドゥ・リドゥ
- Ctrl+Z / Ctrl+Y で操作
- 履歴は最低 50 ステップ保持
- レイヤー操作・シンボル配置・配線・プロパティ変更すべてが対象

#### F-14 PDF 出力
- 用紙サイズ：A4 / A3 / A2 / A1
- 向き：縦 / 横
- 余白・タイトル枠（現場名・作成日・縮尺）の有無を選択
- 出力レイヤーを選択可能
- 印刷プレビュー機能を持つこと

#### F-21 見積管理アプリとの連携
- 拾い出し結果を JSON または CSV 形式で出力
- 見積管理アプリ側で取り込み可能なフォーマットを定義（後述）
- 直接連携（API 経由）は Phase 3 で検討。MVP では CSV エクスポート/インポート方式で実現

---

## 4. シンボル定義

### 4.1 採用記号体系
JIS C 0303（構内電気設備の配線用図記号）に準拠する。すべてを実装する必要はなく、現場で頻出するものを優先する。

### 4.2 MVP で実装するシンボル

#### 照明
| 種別 | 記号概要 | プロパティ |
|---|---|---|
| 一般照明 | ○ | 回路番号・W 数・型番 |
| ダウンライト | DL を内包した○ | 回路番号・W 数・φ・型番 |
| 蛍光灯（直管） | 長方形 | 回路番号・W 数・本数・型番 |
| シーリングライト | × を内包した○ | 回路番号・W 数・型番 |
| 引掛シーリング | (R) | 回路番号 |
| ペンダント | (P) | 回路番号・型番 |
| 屋外灯 | ○W | 回路番号・W 数・型番 |
| 非常照明 | EL | 回路番号 |
| 誘導灯 | 矢印付き四角 | 回路番号・型番 |

#### スイッチ
| 種別 | 記号概要 | プロパティ |
|---|---|---|
| 単極スイッチ | ● | 連用個数・回路番号 |
| 3 路スイッチ | ●3 | 連用個数・回路番号 |
| 4 路スイッチ | ●4 | 連用個数・回路番号 |
| 調光スイッチ | ●D | 回路番号 |
| 自動点滅器 | ●A | 回路番号 |
| センサースイッチ | ●RAS | 回路番号 |
| パイロットスイッチ | ●L | 回路番号 |

#### コンセント
| 種別 | 記号概要 | プロパティ |
|---|---|---|
| 一般コンセント | 半円 | 口数・接地有無・回路番号 |
| 防水コンセント | WP 付半円 | 口数・接地・回路番号 |
| 接地極付 | E 付半円 | 口数・回路番号 |
| アース付 | EE 付半円 | 口数・回路番号 |
| 200V コンセント | 200V 表記 | 容量・回路番号 |
| 床コンセント | □ | 口数・回路番号 |

#### 弱電・通信
| 種別 | 記号概要 | プロパティ |
|---|---|---|
| TV 端子 | TV | 直列/末端 |
| LAN | LAN | カテゴリ |
| 電話 | TEL | |
| インターホン | IH | 親機/子機 |
| ドアホン | DH | |

#### その他設備
| 種別 | 記号概要 | プロパティ |
|---|---|---|
| 換気扇 | ⊕ | 回路番号・型番・風量 |
| エアコン用コンセント | AC | 容量・回路番号 |
| 給湯リモコン | HW | |
| 火災感知器（煙） | S | |
| 火災感知器（熱） | T | |
| 分電盤 | □に斜線 | 回路数 |

### 4.3 シンボルデータ構造
各シンボルは以下のメタデータを保持する。

```typescript
interface Symbol {
  id: string;              // UUID
  type: SymbolType;        // 種別 enum
  layer: string;           // 所属レイヤー ID
  position: { x: number; y: number };  // mm 単位
  rotation: number;        // 度
  scale: number;           // 拡大率（既定 1.0）
  properties: {
    circuit?: string;      // 回路番号
    wattage?: number;      // W 数
    model?: string;        // 型番
    note?: string;         // メモ
    [key: string]: any;    // 種別固有のプロパティ
  };
  countAs: number;         // 拾い出しでの計上数（既定 1）
}
```

### 4.4 シンボル登録の拡張性
- シンボル定義は外部 JSON ファイルで管理する（`symbols.json` など）
- ユーザーが独自シンボル（オリジナルアイコン）を追加できる仕組みを将来的に持たせる

---

## 5. データモデル

### 5.1 プロジェクトファイル構造（`.dkz` 形式（仮））
内部的には ZIP 圧縮された以下の構成。

```
project.dkz/
├── meta.json           # プロジェクトメタデータ
├── drawing/            # 元図面ファイル
│   └── original.pdf
├── data.json           # 書き込みデータ（シンボル・配線・レイヤー）
└── thumbnail.png       # サムネイル
```

### 5.2 主要データ構造

```typescript
interface Project {
  meta: {
    id: string;
    name: string;            // 現場名
    client: string;          // 元請け名
    address: string;         // 現場住所
    createdAt: string;
    updatedAt: string;
    appVersion: string;
  };
  drawing: {
    type: 'pdf' | 'image';
    filename: string;
    pageCount?: number;
    selectedPage?: number;
    width: number;           // mm
    height: number;          // mm
    scale: {                 // F-04 スケール設定の結果
      pixelDistance: number;
      realDistance: number;  // mm
    };
  };
  paper: {
    size: 'A4' | 'A3' | 'A2' | 'A1';
    orientation: 'portrait' | 'landscape';
    drawingScale: string;    // "1/50" など
  };
  layers: Layer[];
  symbols: Symbol[];
  wires: Wire[];
  texts: TextAnnotation[];
}

interface Layer {
  id: string;
  name: string;
  color: string;             // HEX
  visible: boolean;
  locked: boolean;
  zIndex: number;
}

interface Wire {
  id: string;
  layer: string;
  from: string;              // Symbol.id
  to: string;                // Symbol.id
  waypoints: { x: number; y: number }[];
  type: 'ceiling' | 'floor' | 'concealed' | 'exposed';
  cable: string;             // "VVF1.6", "VVF2.0" など
  circuit: string;
  length: number;            // mm（自動算出）
}
```

### 5.3 拾い出し出力フォーマット（CSV）
見積管理アプリとの連携用。

```csv
種別,規格,数量,単位,回路番号,備考
ダウンライト,LED 7W φ100,14,個,L-1〜L-3,
シーリングライト,LED 6畳,3,個,L-4,
単極スイッチ,連用1個,8,個,,
3路スイッチ,連用1個,2,個,,
一般コンセント,2口接地付,18,個,P-1〜P-3,
配線,VVF1.6×2C,62,m,,
配線,VVF2.0×2C,15,m,,
```

---

## 6. UI/UX 要件

### 6.1 画面構成
3 カラムレイアウト（議論済みのモックアップに準拠）。

```
┌──────────────────────────────────────────────────┐
│ メニューバー（ファイル/編集/表示/レイヤー/出力/ヘルプ） │
├──────────────────────────────────────────────────┤
│ ツールバー（選択/配置/配線/寸法/テキスト/ズーム情報）   │
├──────┬───────────────────────────┬──────────┤
│シンボル│                              │ レイヤー   │
│パレット │       図面キャンバス          │           │
│       │                              │ プロパティ │
│       │                              │           │
│       │                              │ 拾い出し  │
├──────┴───────────────────────────┴──────────┤
│ ステータスバー（用紙/縮尺/グリッド/自動保存時刻）       │
└──────────────────────────────────────────────────┘
```

### 6.2 操作モード
ツールバーで切り替える基本モードは以下の 5 つに限定する。
1. 選択モード
2. シンボル配置モード
3. 配線モード
4. 寸法モード（Phase 3）
5. テキストモード（Phase 3）

### 6.3 キーボードショートカット
PC 特化のため、ショートカットは充実させる。

| キー | 動作 |
|---|---|
| Ctrl+N | 新規プロジェクト |
| Ctrl+O | プロジェクトを開く |
| Ctrl+S | 上書き保存 |
| Ctrl+Shift+S | 名前を付けて保存 |
| Ctrl+Z / Ctrl+Y | アンドゥ / リドゥ |
| Ctrl+C / Ctrl+V | コピー / ペースト |
| Ctrl+A | 現在レイヤー全選択 |
| Delete | 選択削除 |
| ESC | モード解除・選択解除 |
| Space + ドラッグ | キャンバスのパン |
| Ctrl+ホイール | ズーム |
| Ctrl+E | PDF エクスポート |
| 矢印キー | 1mm 移動 |
| Shift+矢印 | 10mm 移動 |

### 6.4 デザイン原則
- フラット・ミニマル。装飾を排しタスクに集中できるレイアウト。
- 色は意味のある場所だけに使う（レイヤー色、選択ハイライト、警告）。
- 重要な状態は常に画面のどこかに表示（座標、選択数、自動保存時刻）。
- 日本語フォントを優先。等幅は座標表示にのみ使用。

---

## 7. 非機能要件

### 7.1 性能
- A1 サイズの PDF（3MB 程度）の読み込みを 3 秒以内
- シンボル 500 個・配線 200 本の状態で操作応答を 100ms 以内
- ズーム・パン操作は 60fps を維持

### 7.2 可用性
- 自動保存（5 分間隔）
- アプリクラッシュ時に最新の自動保存ファイルから復旧可能
- 元図面ファイルは取り込み時にプロジェクト内にコピーし、外部依存を排除

### 7.3 互換性
- Windows 10 / 11（主たる対象）
- macOS 対応は将来検討
- 1920×1080 以上のディスプレイで最適表示

### 7.4 保守性
- 機能ごとにモジュール分割（描画 / データモデル / 永続化 / シンボル定義 / 出力）
- シンボル定義は外部 JSON 化し、コード変更なしで追加可能
- ログ出力機能（不具合調査用）

### 7.5 セキュリティ・プライバシー
- データはローカル PC 内のみで保持。外部送信なし。
- 元図面に個人情報（顧客住所など）が含まれる前提で扱うこと。

---

## 8. 開発フェーズ計画

### Phase 1: PoC（1〜2 週間）
**目的**：技術スタックで本当に作れるかを検証する。

達成条件：
- PDF を背景表示できる
- ダウンライト 1 種類のシンボルをクリック配置できる
- 配置データを JSON で保存できる
- 配置済みシンボルとともに PDF として出力できる

**この段階で判明すべき技術リスク**：
- PDF.js でのレンダリング性能
- Konva.js での座標系・レイヤー管理
- jsPDF + svg2pdf での出力品質

### Phase 2: MVP（1〜2 ヶ月）
**目的**：1 名の電気工事職人に実使用してもらう状態にする。

実装範囲：
- 第 3.1 節の Phase 2 機能すべて
- 主要シンボル 20 種類程度
- レイヤー管理・配線描画・拾い出し集計
- PDF 出力・CSV 出力

ユーザーテスト：
- 北嶺建設の協力電気工事職人 1 名にβ版として提供
- 実際の現場 2〜3 件で使用してもらいフィードバック取得

### Phase 3: 実用版（さらに 2〜3 ヶ月）
- 全シンボル対応
- テンプレート機能
- 寸法計測・直角配線
- 見積管理アプリとの連携
- 印刷の作り込み

### Phase 4: 拡張（任意）
- iPad 対応検討
- クラウド同期
- 複数現場の管理ダッシュボード

---

## 9. 開発上の注意点・既知の難所

### 9.1 技術的難所
Claude Code が設計時に注意すべきポイント。

1. **座標系の二重管理**
   - スクリーン座標（ピクセル）と実寸座標（mm）の変換が頻出
   - スケール変更・ズーム・パンが絡むと混乱しやすい
   - 早い段階で座標変換ユーティリティを整備し、UI コードから直接ピクセル計算しないこと

2. **アンドゥ・リドゥの設計**
   - Command パターンで操作を記録するか、状態スナップショットを保持するか早期に決定
   - 複合操作（複数シンボルを一括移動など）の扱いに注意

3. **大きな PDF のメモリ管理**
   - ページ単位でレンダリングし、ビューポート外は破棄する設計を検討
   - PDF.js の `getPage()` と `render()` のキャッシュ戦略を初期から意識

4. **配線とシンボルの参照整合性**
   - シンボル削除時に、それを参照する配線の扱いを定義
   - シンボル ID は UUID とし、再生成しないこと

5. **自動保存とアンドゥの干渉**
   - 自動保存中にアンドゥが走ると不整合の可能性
   - 排他制御の設計を初期から考慮

### 9.2 UI 上の難所
1. **左右パネルのリサイズ**
   - 後から実装すると工数が膨らむ。初期から対応する設計に
2. **ミニマップの同期**
   - メインキャンバスとの双方向連動。スロットリング必須
3. **プロパティパネルの動的切替**
   - シンボル種別ごとにフォーム定義（スキーマ駆動）にすること

### 9.3 仕様未確定事項（着手前に Daisuke と協議）
- [ ] Electron / Tauri のいずれを採用するか（既存見積管理アプリの選定に合わせる）
- [ ] 状態管理ライブラリの選定
- [x] プロジェクトファイル拡張子の正式名称 → 仮で `.dkz`、第 1 段階終了時に確定
- [ ] 見積管理アプリとの連携 I/F の正式仕様
- [ ] アイコン・シンボルのデザイン担当（既製ライブラリ流用 or 独自作成）
- [x] アプリ名称の正式決定 → 仮称「Denkeez（デンキーズ）」、第 1 段階終了時に確定
- [ ] Denkeez 仮称の正式採用可否（ドメイン取得可能性・商標調査・既存サービスとの衝突確認）
- [ ] β版テストに協力する職人さんの確保（誠一氏に打診中）

---

## 10. 完了の定義（Definition of Done）

### 10.1 Phase 1 完了条件
- [ ] PoC アプリが起動する
- [ ] サンプル PDF を読み込んで表示できる
- [ ] ダウンライトを 10 個以上配置・移動・削除できる
- [ ] 配置データを保存・再読込できる
- [ ] 配置済み図面を PDF 出力できる
- [ ] 上記が Daisuke の手元 PC で動作確認済み

### 10.2 Phase 2 完了条件
- [ ] 第 3.1 節 Phase 2 までの全機能が動作する
- [ ] β版が職人 1 名に配布されている
- [ ] 実案件 1 件以上で使用された
- [ ] フィードバック票 5 件以上回収済み
- [ ] クリティカルバグが 0 件

---

## 11. Claude Code 向け補足

### 11.1 進め方の指針
1. 本書を読み込んだ後、**まず Plan モードで Phase 1 の詳細設計を提示**してください。
2. ファイル構成・ディレクトリ構成の提案を含めること。
3. 主要な技術選定（特に第 9.3 節の仕様未確定事項）について、判断材料を整理した上で Daisuke の判断を仰ぐこと。
4. 着手前に「動かない可能性のある前提」を洗い出し、リスク表として提示すること。
5. テスト戦略（単体テスト・結合テスト・手動テスト）を最初に決めること。

### 11.2 コーディング規約
- TypeScript strict モード
- ESLint + Prettier 適用
- コメントは日本語可。ただし変数名・関数名は英語。
- 1 ファイル 300 行以内を目安に分割
- 副作用を持つ関数は明確に分離

### 11.3 ドキュメント
- README.md にビルド手順・起動手順を必ず記載
- 主要モジュールには JSDoc コメント
- アーキテクチャ概要図を初期段階で作成し `docs/` に配置

### 11.4 リポジトリ運用
- Git 管理。コミットメッセージは日本語可（Conventional Commits 準拠推奨）
- main ブランチは常にビルド可能な状態を保つ
- 機能追加は feature ブランチで開発し、PR でマージ

---

**本書の改訂履歴**

| 版 | 日付 | 内容 | 担当 |
|---|---|---|---|
| 0.1 | 2026-04-28 | 初版作成 | Daisuke + Claude |
| 0.2 | 2026-04-29 | プロダクト仮称「Denkeez（デンキーズ）」を採用。ファイル拡張子を `.dkz`（仮）に。誠一氏への謝辞を明記。仕様未確定事項を更新。 | Daisuke + Claude |
| 0.3 | 2026-04-29 | プロジェクト概要（第 1.1 節）から謝辞の記載を削除。 | Daisuke + Claude |
